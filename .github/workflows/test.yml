name: Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Run all test scenarios
      run: |
        cd tests
        chmod +x run_scenario.sh
        ./run_scenario.sh
